import 'package:flutter/material.dart';

class Skill {
  final Color color;
  final String title;
  final double maxScore;

  Skill({
    required this.color,
    required this.title,
    required this.maxScore,
  });
}
